export enum GoogleRecaptchaTheme {
  DARK = 'dark',
  LIGHT = 'light'
}

export enum GoogleRecaptchaSize {
  NORMAL = 'normal',
  COMPACT = 'compact',
  INVISIBLE = 'invisible'
}

export enum GoogleRecaptchaActionName {
  LOGIN = 'LOGIN',
  PASSWORD_RESET = 'PASSWORD_RESET',
  ADD_TO_CART = 'ADD_TO_CART',
  CHECKOUT = 'CHECKOUT',
  GET_PRICE = 'GET_PRICE',
  PLAY_SONG = 'PLAY_SONG'
}
